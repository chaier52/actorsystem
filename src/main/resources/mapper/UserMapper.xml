<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaee.mapper.UserMapper">

    <select id="listAllUsers" resultType="com.javaee.po.User">
        SELECT
            user_id    AS userId,
            username,
            password,
            role
        FROM `user`
    </select>

    <select id="getUserById" resultType="com.javaee.po.User">
        SELECT 
            user_id AS userId,
            username,
            password,
            role
        FROM user 
        WHERE user_id = #{userId}
    </select>

    <select id="selectUserByUsername" resultType="com.javaee.po.User">
        SELECT 
            user_id AS userId,
            username,
            password,
            role
        FROM user 
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.javaee.po.User">
        INSERT INTO user (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

    <update id="updateUser" parameterType="com.javaee.po.User">
        UPDATE user 
        SET username = #{username}, 
            password = #{password},
            role = #{role}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>
    
    <update id="updatePassword" parameterType="com.javaee.po.User">
        UPDATE user 
        SET password = #{password} 
        WHERE user_id = #{userId}
    </update>

</mapper>